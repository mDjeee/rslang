import { SprintBorderDirective } from './sprint-border.directive';

describe('SprintBorderDirective', () => {
  it('should create an instance', () => {
    // @ts-ignore
    const directive = new SprintBorderDirective();
    expect(directive).toBeTruthy();
  });
});
